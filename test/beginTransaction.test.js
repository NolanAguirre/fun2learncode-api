const beginTransaction = require('../beginTransaction');

describe('begin transaction tests', () => {
    test('fail auth', ()=> {

    })

    test('pass auth, fail x', () => {

    })
})

test('adds 1 + 2 to equal 3', (done) => {
  expect(sum(1, 2)).toBe(3);
});
