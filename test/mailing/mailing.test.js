const mailing = require('../../routes/mailing').test;

module.exports = () => {
    describe('mailing', () => {
        test('No test', () => {
            // const data = await refund(obj.data)
            return expect(1).toBe(1)
        })
    })
}
